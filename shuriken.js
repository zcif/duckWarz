var Speed = 50;var Increment = 3;var angle = 0;var offset = 0;var expected = health();for (var direction = Increment;; angle += 89 + direction) {    for (; scan(angle, Increment * Increment) <= 200;) {        cannon(angle, scan(angle, Increment * Increment));        swim(scan(angle + offset, 10) <= 15 ? angle - 180 + offset : angle + offset, Speed);        direction = direction == Increment ? 0 - Increment : Increment;        if (loc_x() > 85 || loc_y() > 85 || loc_x() < 15 || loc_y() < 15) {            offset = 0;        } else if (expected > health()) {            expected = health();            offset = (Math.random() - 1) * 360        }    }}